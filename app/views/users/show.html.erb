<h1><%= @user.email %>'s Profile</h1>

<h2>Created Events</h2>
<ul>
  <% @created_events.each do |event| %>
    <li><%= event.location %> on <%= event.date %></li>
  <% end %>
</ul>

<h2>Attended Events</h2>
<ul>
  <% @user.attended_events.each do |event| %>
    <li><%= event.location %> on <%= event.date %></li>
  <% end %>
</ul>